# Required for any "Try it" call. Get yours at https://www.last.fm/api/account/create
LASTFM_API_KEY=your-api-key

# Required for signed methods (auth.getToken, auth.getSession,
# and any write method — track.scrobble, track.love, track.addTags, ...).
LASTFM_SHARED_SECRET=your-shared-secret

# Optional — only as a fallback. Prefer the per-request `x-lastfm-sk` header
# so the key is never written to disk. See the auth tag in the Scalar UI
# for the full flow.
LASTFM_SESSION_KEY=

# Port the local docs server binds to.
PORT=3000
